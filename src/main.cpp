#include <iostream>
#include <vector>
#include <memory>
#include "Tiles.h"


int main() {
    
    TileList gameBoard(1);
    gameBoard.printBoard();

    // TODO:
        // resolve the correct input for board[y*rowsize + x]

}